# sulu-admin-bundle is a peer dependency that ships inside the Composer package, not on the npm
# registry, so npm 7+ cannot auto-install it. Only the bundle's own test run installs from here;
# a consuming project installs this package by path and satisfies the peer from its own tree.
legacy-peer-deps=true
